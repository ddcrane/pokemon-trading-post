
<div class="col-auto card-container container-fluid d-flex">
    <div class="card-info">
        <img class="card-img" src="{{image_url}}" alt="">
        <p class="card-atrs">Set: {{set_name}}</p>
        <p class="card-atrs">Rarity: {{card_rarity}}</p>
    </div>
</div> 